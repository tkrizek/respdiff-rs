image: registry.nic.cz/knot/respdiff-rs/ci

variables:
  LC_ALL: C.UTF-8

stages:
  - build
  - test

rust-latest:
  stage: build
  script:
    - cargo build --verbose
    - cargo test --verbose

rust-nightly:
  stage: build
  image: rustlang/rust:nightly
  script:
    - cargo build --verbose
    - cargo test --verbose
  allow_failure: true

fmt:
  stage: test
  script:
    - cargo fmt -- --check

clippy:
  stage: test
  script:
    - cargo clippy -- -D "warnings"
